export class CheckConditionComponent {
  constructor(entity, data) {
    this.entity = entity;
    // console.log(conditions);
    this.conditions = data.conditions; // List of conditions to check
  }
  // Move to system
}
